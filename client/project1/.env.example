# RentlyX frontend environment configuration
# Copy this file to .env and adjust the URLs for your deployment.

VITE_DJANGO_ORIGIN=https://api.yourdomain.com
# Keep this only if you still run a separate AI service.
VITE_AI_ORIGIN=https://api.yourdomain.com
